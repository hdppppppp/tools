# 安全说明

本文档说明这套加密层**防什么、不防什么**，以及为什么这样选型。
结论先行：它是一道**抬高门槛**的应用层防线，不是「密钥不可提取」的方案。

---

## 一、威胁模型

### 防

| 威胁 | 手段 |
| --- | --- |
| 抓包读取载荷（歌曲直链、用户资料、歌单名） | ChaCha20-Poly1305 加密请求/响应体 |
| 篡改请求参数 | AEAD 认证标签；AAD 绑定到具体端点 |
| 重放请求 | 时间戳窗口 + 握手 nonce 缓存 + 会话序号滑窗（三层） |
| 把 A 端点的密文重放到 B 端点 | AAD 里含 `"{METHOD} {PATH}?{QUERY}"` |
| 中间人替换公钥 | PSK 参与握手认证（HMAC），攻击者没有 PSK 就算不出合法 MAC |
| 抓包 + 改脚本模拟请求的「半小时工作量」 | 让这件事需要真正理解协议 + 提取密钥 + 重建握手 |
| 长期密钥泄露后解密历史流量 | X25519 每次握手生成临时密钥对，提供前向保密 |

### 不防

| 威胁 | 原因 |
| --- | --- |
| 反编译客户端后提取 PSK | **PSK 模型的固有性质**，见下一节 |
| 内存转储（Frida / 调试器附加） | 密钥在进程内存里必然以明文存在 |
| 有 PSK 的攻击者伪造合法请求 | 这是 PSK 的定义，不是漏洞 |
| 流量分析（谁在什么时候和谁通信、包多大） | 未做填充与流量整形 |
| 服务端被完全攻陷 | 加密层管不了已经拿到服务端权限的攻击者 |
| 设备被 root / 越狱后的运行时注入 | 超出本层能力范围 |

---

## 二、关于 PSK 可提取性的诚实说明

**PSK 在客户端产物里是可提取的。** 分片混淆（`core/src/psk_blob.rs`）把 32 字节
密钥拆成 4 片、按逆序存放、各片与不同掩码异或 —— 这只让「用 `strings` 扫一遍」
不再奏效，**不提供任何密码学意义上的保密**。

这不是实现缺陷，而是 PSK 模型的必然结果：客户端必须持有密钥才能完成握手，
任何持有密钥的客户端都能被逆向出密钥。换成证书、换成密钥协商、
换成任何方案，只要客户端需要长期凭据，这个结论都不变。

由此得到的实际约束：

1. **含真密钥的产物不能公开分发。** CI（`ci.yml`）因此用占位密钥构建，
   `hasRealPsk()` 返回 `false`，产物可以放心公开；真密钥只在
   `release.yml` 里从 GitHub Secrets 注入。
2. **不要四端共用一个 PSK 跨越信任边界。** 客户端 PSK 泄露是时间问题，
   它一旦泄露，就等于服务端的校验密钥泄露。轮换机制已经支持
   （`ServerEngine::put_psk` 可同时持有多条），应当纳入运维计划。
3. **运行时校验 `hasRealPsk()`。** 服务端启动时必须检查它，
   为 `false` 就拒绝启动 —— 否则会在「看起来一切正常」的状态下用占位密钥
   接受所有请求。

---

## 三、密码学选型

| 用途 | 算法 | 为什么不是别的 |
| --- | --- | --- |
| AEAD | ChaCha20-Poly1305 | 见下 |
| 密钥协商 | X25519 | 广泛实现、常量时间、密钥小 |
| 密钥派生 | HKDF-SHA256 | 标准做法；双向密钥用不同 info 标签 |
| 握手认证 | HMAC-SHA256 | PSK 参与，防中间人 |
| 常量时间比较 | `subtle` crate | 手写比较会引入时序侧信道 |

### 为什么是 ChaCha20-Poly1305 而不是 AES-256-GCM

两个具体原因，都与本项目的四目标有关：

1. **wasm 没有 AES 硬件指令。** WebAssembly 的 SIMD 里没有 AES-NI 等价物，
   AES 在浏览器里只能走软件实现，性能差且实现质量取决于编译器。
2. **老安卓机型的 AES 会退化成查表实现。** 没有 AES-NI 的 ARM 设备上，
   软件 AES 通常用 T 表实现，而 T 表查表的访存地址与密钥相关 ——
   这是经典的缓存计时侧信道（Bernstein 2005）。ChaCha20 是纯 ARX 运算
   （加、异或、循环移位），天然常量时间。

代价是：在有 AES-NI 的现代 x86 上，ChaCha20 比 AES-GCM 慢一些。
对本场景（请求体是几十 KB 的 JSON，不是音频流）完全无所谓。

### 双向密钥必须用不同的 HKDF info

`core/src/kdf.rs` 里 `INFO_KEY_C2S` 与 `INFO_KEY_S2C` 是两个不同的标签。
如果两端派生出同一把密钥，攻击者可以把客户端发往服务端的密文原样回弹给
客户端自己 —— 反射攻击。两条链路的密钥不同，这个攻击就不成立。

---

## 四、会话生命周期

| 参数 | 值 | 理由 |
| --- | --- | --- |
| 会话 TTL | 30 分钟 | 够长以避免频繁握手，够短以限制单会话密钥的暴露窗口 |
| 提前 rekey | 剩余 10% 时 | 避免 TTL 到期瞬间所有客户端同时重握手（惊群） |
| 单会话帧上限 | 2^24（约 1677 万） | nonce 生日界，见下 |
| 时间戳窗口 | ±5 分钟 | 允许合理的时钟偏差，同时限制重放窗口 |
| 握手 nonce 缓存 | 10 分钟 | 覆盖时间戳窗口的两倍，避免窗口边缘的重放 |
| 服务端会话上限 | 100,000 | 防内存耗尽 |

### 为什么单会话有 2^24 帧上限

帧 nonce 是 96 位随机值。在随机 nonce 下，碰撞概率按生日界估算：
使用 2^32 个帧时，碰撞概率约 2^-33；2^24 个帧时约 2^-49。
后者是可以忽略的量级，前者已经在「值得担心的工程风险」范围内。
上限达到时强制重新握手，换一把会话密钥。

### 校验顺序：时间戳 → 解密 → 防重放

防重放**必须放在最后**。反过来的话，攻击者可以大量发送伪造帧，
每一条都会在滑窗里占据一个序号，把窗口推满 —— 合法帧反而被当作重放拒掉。
先做时间戳检查（廉价）再做解密（昂贵且能验真伪），伪造帧在解密阶段就被
挡掉，根本走不到滑窗。

---

## 五、防重放的三层

1. **时间戳窗口**：帧里的 `ts_ms` 与本地时间差超过 ±5 分钟直接拒。
   只能挡住明显过期的重放，攻击者可以在窗口内立即重放。
2. **握手 nonce 缓存**：ClientHello 的 nonce 在 10 分钟内不能重复。
   挡住重放整个握手（否则攻击者能重放握手让服务端建立大量无用会话）。
   **缓存插入在 MAC 校验之后** —— 否则攻击者用垃圾 hello 就能把缓存灌满，
   合法客户端反而建不了会话。
3. **会话序号滑窗**：64 位滑窗（`core/src/frame.rs` 的 `ReplayWindow`）。
   允许乱序容忍（网络本来就会乱序），但拒绝任何已经处理过的序号。

---

## 六、已知限制

- **不加密音频流与大文件上传。** 音频代理、APK/jar 下载、图片直链、
  `/search` 的裸 NDJSON 流式响应都**不在加密范围内** —— 它们要么是流式的
  （逐帧加密需要改动客户端解码链路），要么本身就是二进制大对象。
  豁免清单见 `bindings/*/index.ts` 里的 `ENCRYPTION_EXEMPT_PATTERNS`。
- **不替代 TLS。** 这是应用层的第二道防线。TLS 保护传输，本层保护
  应用数据不被「已经能读到明文 HTTP 的中间人」利用。
- **抗逆向只到「抬高成本」为止。** 没有做控制流平坦化、没有反调试、
  没有白盒密码学。做这些的收益递减，而维护成本很高。
- **PSK 只支持构建期注入。** 运行时注入路径（从密钥管理服务在启动时拉取）
  尚未实现，见 `docs/design.md` 的「后续工作」。
- **`bindings/` 下的三个包装层没有被编译验证过。** 没有接入环境，
  接入时要先跑通再依赖。

---

## 七、报告问题

这是私有项目，不接受外部报告。内部发现安全问题请在仓库开 issue 并
打 `security` 标签，或直接联系仓库维护者。

**不要在 issue 里粘贴真实 PSK 或密钥材料。**
