# ---- Rust 构建产物 ----
# target/ 有数千个中间文件、体积以 GB 计，绝不入库。
target/
dist/

# ---- 本地密钥 ----
# PSK 走构建期环境变量注入，任何落盘形式都是事故。
# 这几条是防手滑的兜底，正常流程里不会出现这些文件。
.env
.env.*
!.env.example
*.psk
psk.txt
secrets/

# ---- 编辑器与系统 ----
.vscode/
.idea/
*.iml
*.swp
*.swo
.DS_Store
Thumbs.db

# ---- 注意：以下文件**必须**入库，不要加进来 ----
# Cargo.lock    —— wasm-bindgen CLI 的版本必须与它一致，
#                  不一致时 wasm-bindgen 会「成功」但一个文件都不产出。
# .cargo/       —— Android 交叉编译的链接器与 16KB 页面对齐配置。
